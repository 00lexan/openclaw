---
summary: "CLI reference for ` uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# ` uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
 uninstall
 uninstall --all --yes
 uninstall --dry-run
```

